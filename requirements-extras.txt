# Claudy desktop — dependencias OPCIONALES
# Instalar: pip install -r requirements-extras.txt
# Solo necesarias para funciones específicas; Claudy arranca sin ellas.

# Dictado por micrófono en el escritorio (push-to-talk)
SpeechRecognition>=3.10
PyAudio>=0.2.13
playsound==1.2.2

# Visión / OCR
pytesseract>=0.3.10

# Web / browser automation / YouTube
playwright>=1.40
yt-dlp>=2024.0
youtube-transcript-api>=0.6

# MCP (Model Context Protocol)
mcp>=1.0

# Audio del sistema (control de volumen)
pycaw>=20240210
comtypes>=1.2

# Drag & drop de archivos al chat
tkinterdnd2>=0.4

# Analytics / embeddings / gráficos
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
sentence-transformers>=2.5
numpy>=1.24

# Webcam
# opencv-python>=4.8

# Proveedor de memoria externo
# mem0ai>=0.1
