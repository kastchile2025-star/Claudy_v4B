# Claudy desktop — dependencias CORE (lo mínimo para arrancar)
# Instalar: pip install -r requirements.txt
# Extras (voz por micrófono, visión, web automation, analytics): requirements-extras.txt

# Núcleo
Pillow>=10.0
requests>=2.31
pywebview>=5.0

# Telegram (único canal externo)
python-telegram-bot>=21.0

# Voz del bot y del escritorio
edge-tts>=7.0
faster-whisper>=1.0

# Documentos (docx/xlsx/pptx/pdf que genera Claudy)
python-docx>=1.0
openpyxl>=3.1
python-pptx>=0.6
reportlab>=4.0
pypdf>=4.0

# Seguridad (secure_store: secretos encriptados en config.json)
cryptography>=41.0

# Sistema
keyboard>=0.13
mss>=9.0
psutil>=5.9
pystray>=0.19
winotify

# Google Calendar (panel Calendario + /agendar)
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
